# Static readback validation marker

Temporary non-merge marker used to trigger exact-tree CI and static readback workflows for current `main` after PR #3311. It changes no runtime source, migration, schema, provider state, credentials, deployment configuration, or Production branch.
