# CI Autostart Smoke

Temporary same-repository PR marker used to verify that CI autostart recovery dispatches `ci.yml` without manual workflow dispatch.

- Scope: smoke validation only
- Secrets included: false
- Deploy impact: none
