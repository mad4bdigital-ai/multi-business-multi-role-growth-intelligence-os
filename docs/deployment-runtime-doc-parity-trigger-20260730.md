# Deployment Runtime Documentation Parity Trigger

This disposable marker exists only to trigger the bounded self-removing documentation workflow on the documentation branch. It must not be merged into `main` and contains no runtime, deployment, SQL, provider, credential, or external-write behavior.
