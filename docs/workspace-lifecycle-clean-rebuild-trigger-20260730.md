# Workspace Lifecycle final synchronization trigger

Temporary non-runtime marker used only to trigger the exact-branch governed final synchronization. The workflow must delete this file before pushing the synchronized delivery branch.
