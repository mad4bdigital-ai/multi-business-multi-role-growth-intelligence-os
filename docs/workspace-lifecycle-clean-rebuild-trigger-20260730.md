# Workspace Lifecycle clean rebuild trigger

Temporary non-runtime marker used only to open the exact-branch pull request that triggers the bounded clean rebuild workflow. This file must be deleted before final review and merge.
