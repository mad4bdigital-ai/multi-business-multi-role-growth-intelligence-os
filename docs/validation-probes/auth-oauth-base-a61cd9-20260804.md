# Exact OAuth base probe

Temporary non-merge validation marker for `main@a61cd9e83070618001676bbc679bc937f0551875`.

The paired read-only ARM workflow checks out that immutable commit directly and runs only `http-generic-api/test-auth-oauth-routes.mjs`. No production, provider, credential, database, SQL, migration, external-send, deployment, restart, protected-ref, or force-push action is authorized.
