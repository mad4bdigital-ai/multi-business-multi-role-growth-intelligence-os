{
  "$schema": "../../.specify/schemas/e2e-phases.schema.json",
  "schema_version": 1,
  "feature_key": "staging-windows-cloudflared-http2-20260902",
  "title": "Pin the Windows Staging Cloudflared transport to HTTP/2",
  "delivery_mode": "single_pr",
  "merge_contract": {"minimum_phase": "mvp"},
  "current_phase": "mvp",
  "scope": {"include": [
    ".changes/e2e/staging-windows-cloudflared-http2-20260902.json",
    "autopilot-portable-staging/Staging-WindowsCloudflared.ps1",
    "autopilot-portable-staging/manifest.json",
    "http-generic-api/scripts/staging-dual-mode-one-click-contract.mjs",
    "http-generic-api/scripts/staging-platform-ready-contract.mjs"
  ]},
  "environment_impact": {
    "source_of_truth": "http-generic-api/config/deployment-branch-policy.json",
    "declared_targets": ["staging"],
    "cross_environment_reviewed": true,
    "live_staging_certification_required": true,
    "production_mutation_allowed": false
  },
  "phases": [{
    "id": "mvp",
    "status": "implemented",
    "objective": "Pin the Windows Staging Cloudflared service transport to HTTP/2 so repeated remote QUIC closures cannot rotate the service process inside the required stability window, while preserving token-file transport, LocalSystem, automatic start, bounded recovery, fresh-log origin proof, loopback-only metrics, and fail-closed service readback.",
    "e2e_journeys": [{
      "id": "windows-cloudflared-http2-stability",
      "end_to_end": true,
      "level": "synthetic_runtime",
      "actor": "Local Windows Docker Staging operator",
      "entrypoint": "autopilot-portable-staging/Invoke-Staging-One-Click.ps1 -TunnelMode windows_service",
      "terminal_outcome": "The reconciled Cloudflared Windows service is pinned to HTTP/2, retains one process id throughout the configured stability window, and proceeds to remote-origin proof and PLATFORM_READY.",
      "steps": [
        "Build the Windows service command line with tunnel --protocol http2 before run.",
        "Reconcile the exact command line through Win32_Service.Change without embedding an inline token.",
        "Fail closed unless service PathName readback proves the canonical HTTP/2 transport.",
        "Expose the selected transport in bounded non-secret reconciliation evidence.",
        "Require live Staging certification after merge before treating windows_service mode as platform-ready."
      ],
      "assertions": [
        "Windows service transport is exactly HTTP/2 and is verified from PathName readback.",
        "Docker sidecar topology and its loopback origin behavior remain unchanged.",
        "Token transport remains the protected canonical token-file and never becomes an inline token.",
        "The service remains LocalSystem, automatic-start, loopback-metrics-only, and bounded-recovery.",
        "No Cloudflare DNS mutation, Production deployment, Production access, database mutation, migration apply, ruleset mutation, secret rotation, container deletion, or volume deletion is authorized."
      ],
      "tests": [
        {"id": "staging-dual-mode-one-click-contract", "runner": "node", "working_directory": "http-generic-api", "path": "scripts/staging-dual-mode-one-click-contract.mjs", "args": []},
        {"id": "staging-platform-ready-contract", "runner": "node", "working_directory": "http-generic-api", "path": "scripts/staging-platform-ready-contract.mjs", "args": []}
      ],
      "evidence_paths": [
        ".changes/e2e/staging-windows-cloudflared-http2-20260902.json",
        "autopilot-portable-staging/Staging-WindowsCloudflared.ps1",
        "autopilot-portable-staging/manifest.json",
        "http-generic-api/scripts/staging-dual-mode-one-click-contract.mjs",
        "http-generic-api/scripts/staging-platform-ready-contract.mjs"
      ]
    }]
  }],
  "secrets_included": false
}
