# Copy to .env.staging and keep the copy local/untracked.
PORT=8080
APP_ENV_FILE=.env.staging
WORKER_CONCURRENCY=1

# Local-only default: leave database fields blank until a dedicated local DB is approved.
DB_HOST=
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=
GOVERNANCE_DB_USER=
GOVERNANCE_DB_PASSWORD=

# Local route authentication; set a local-only value before testing protected routes.
BACKEND_API_KEY=
DATA_SOURCE=sql

# No external provider secrets in this file.
GITHUB_TOKEN=
GOOGLE_APPLICATION_CREDENTIALS=
REGISTRY_SPREADSHEET_ID=
ACTIVITY_SPREADSHEET_ID=

# Governance defaults: do not enable remotely or mutate locally.
REMOTE_MCP_ENABLED=false
REMOTE_MCP_OAUTH_ENABLED=false
REMOTE_MCP_OAUTH_DCR_ENABLED=false
REMOTE_MCP_LEGACY_USER_JWT_ENABLED=false
REMOTE_MCP_OAUTH_ALLOW_LOOPBACK=false
MIGRATION_APPLIED=false
DATABASE_MUTATED=false
PRODUCTION_MUTATION_AUTHORIZED=false
RULESET_MUTATION_AUTHORIZED=false
