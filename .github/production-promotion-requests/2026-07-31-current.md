# Governed Production synchronization request

This review-only marker requests the trusted `main` workflow to build a source-pinned, exact-main-tree Production promotion candidate from atomic protected-branch readback.

The request itself must not be merged. It does not authorize deployment, SQL, migrations, provider calls, credential reads, service restarts, or direct protected-branch mutation.
