name: Context Kernel Hardcoding Report

on:
  push:
    branches:
      - gpt/post-production-local-connector-closure-20260731

permissions:
  contents: read

concurrency:
  group: context-kernel-hardcoding-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cancel-superseded-closure:
    name: Cancel Superseded Temporary Closure
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary branch closure run superseded by the governed default-branch workflow path."
