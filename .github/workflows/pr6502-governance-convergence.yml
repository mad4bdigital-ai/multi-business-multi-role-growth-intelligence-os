name: PR 6502 Governance Convergence

on:
  push:
    branches:
      - fix/push-workflow-runner-context-registration-main-ba10f269-20260806

permissions:
  contents: write

concurrency:
  group: pr6502-governance-convergence
  cancel-in-progress: false

jobs:
  converge:
    if: github.repository == 'mad4bdigital-ai/multi-business-multi-role-growth-intelligence-os'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout exact renderer head
        uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: true

      - name: Verify bounded branch and expected remote head
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ "${GITHUB_REF_NAME}" == "fix/push-workflow-runner-context-registration-main-ba10f269-20260806" ]]
          [[ "${GITHUB_REF_NAME}" != "main" ]]
          [[ "${GITHUB_REF_NAME}" != "Production" ]]
          current_head_sha="$(git rev-parse HEAD)"
          [[ "${current_head_sha}" == "${GITHUB_SHA}" ]]
          remote_head_sha="$(git ls-remote origin "refs/heads/${GITHUB_REF_NAME}" | awk '{print $1}')"
          [[ "${remote_head_sha}" == "${GITHUB_SHA}" ]]

      - name: Render lifecycle, E2E, and Work Map convergence
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 .github/scripts/pr6502-governance-convergence.py

      - name: Commit exact final tree and push without force
        shell: bash
        run: |
          set -Eeuo pipefail
          git status --short
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && {
            echo "::error::renderer produced no final diff"
            exit 1
          }
          git commit -m "fix(ci): converge workflow lifecycle and E2E governance"
          remote_head_sha="$(git ls-remote origin "refs/heads/${GITHUB_REF_NAME}" | awk '{print $1}')"
          [[ "${remote_head_sha}" == "${GITHUB_SHA}" ]]
          git push origin "HEAD:refs/heads/${GITHUB_REF_NAME}"
