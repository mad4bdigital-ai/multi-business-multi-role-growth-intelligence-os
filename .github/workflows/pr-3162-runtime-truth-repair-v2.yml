name: PR 3162 runtime-truth repair v2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Repair trigger parked; use the verified failed-run rerun for the serial commit attempt."
