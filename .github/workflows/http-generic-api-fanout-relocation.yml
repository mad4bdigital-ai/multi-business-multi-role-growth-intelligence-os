name: HTTP Generic API Fanout Relocation

on:
  workflow_dispatch:
    inputs:
      scope:
        description: "Relocation scope to inspect."
        required: true
        default: "scripts"
        type: choice
        options:
          - scripts
          - all
      max_files:
        description: "Maximum files to classify in one report run."
        required: true
        default: "20"
      target_branch:
        description: "Branch to inspect. Leave empty to use the selected ref."
        required: false
        default: ""
  pull_request:
    paths:
      - "http-generic-api/scripts/**"
      - "http-generic-api/test-fanout-relocation-ci.mjs"
      - ".github/workflows/http-generic-api-fanout-relocation.yml"
      - ".github/workflows/http-generic-api-fanout-relocation-apply.yml"

permissions:
  contents: read
  pull-requests: read

jobs:
  fanout-relocation-report:
    name: Report fanout relocation taxonomy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact pull request head
        if: github.event_name == 'pull_request'
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Checkout selected report ref
        if: github.event_name == 'workflow_dispatch'
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.target_branch || github.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: http-generic-api/package-lock.json

      - name: Install dependencies
        working-directory: http-generic-api
        run: npm ci

      - name: Report fanout relocation taxonomy
        working-directory: http-generic-api
        env:
          FANOUT_RELOCATION_MODE: report
          FANOUT_RELOCATION_SCOPE: ${{ inputs.scope || 'scripts' }}
          FANOUT_RELOCATION_MAX_FILES: ${{ inputs.max_files || '20' }}
          FANOUT_RELOCATION_JSON: "true"
        run: node scripts/fanout-relocation-ci.mjs --json

      - name: Validate relocation tooling
        working-directory: http-generic-api
        run: node test-fanout-relocation-ci.mjs
