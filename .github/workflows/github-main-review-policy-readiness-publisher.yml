name: GitHub Main Review Policy Readiness Publisher

on:
  workflow_run:
    workflows:
      - Governed GitHub Main Review Policy Live Activation
    types: [completed]

permissions:
  actions: read
  contents: read
  issues: write

concurrency:
  group: github-main-review-policy-readiness-publisher-${{ github.event.workflow_run.id }}
  cancel-in-progress: false

jobs:
  publish:
    if: >-
      github.event.workflow_run.event == 'issue_comment' &&
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_sha != null
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout exact source head
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 1
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - name: Discover exact readiness evidence artifact
        id: readiness
        uses: actions/github-script@v7
        with:
          script: |
            const expected = `github-main-review-policy-readiness-${context.payload.workflow_run.id}`;
            const response = await github.rest.actions.listWorkflowRunArtifacts({ owner: context.repo.owner, repo: context.repo.repo, run_id: context.payload.workflow_run.id, per_page: 100 });
            const matches = response.data.artifacts.filter((artifact) => artifact.name === expected && artifact.expired === false);
            if (matches.length > 1) { core.setFailed(`Ambiguous GitHub main review policy readiness artifact: ${matches.length}`); return; }
            core.setOutput('found', matches.length === 1 ? 'true' : 'false');
            core.setOutput('name', expected);
      - name: Download exact readiness evidence
        if: steps.readiness.outputs.found == 'true'
        uses: actions/download-artifact@v4
        with:
          name: ${{ steps.readiness.outputs.name }}
          path: ${{ runner.temp }}/github-main-review-policy-readiness
          repository: ${{ github.repository }}
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ github.token }}
      - name: Publish exact main/fingerprint readiness marker
        if: steps.readiness.outputs.found == 'true'
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          SOURCE_RUN_ID: ${{ github.event.workflow_run.id }}
          SOURCE_HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
          SOURCE_WORKFLOW: ${{ github.event.workflow_run.name }}
          CONTROL_ISSUE: '6625'
          READINESS_SUMMARY_PATH: ${{ runner.temp }}/github-main-review-policy-readiness/summary.json
        run: node http-generic-api/scripts/github-main-review-policy-readiness-issue-publisher.mjs
