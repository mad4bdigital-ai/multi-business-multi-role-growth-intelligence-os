name: Temporary Spec 014 Bootstrap Run Canceller

on:
  pull_request:
    types: [synchronize]
    branches: [main]
    paths:
      - '.github/workflows/tmp-spec014-ddl-materialized-candidate-pr-launcher.yml'

permissions:
  contents: read

concurrency:
  group: tmp-spec014-ddl-materialized-candidate-builder
  cancel-in-progress: true

jobs:
  cancel-superseded-bootstrap:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo cancelled
