name: Dispatch Spec Kit Work Map Autofix 6517

on:
  push:
    branches:
      - ops/dispatch-work-map-autofix-6517-20260807
    paths:
      - .github/workflows/dispatch-spec-kit-work-map-autofix-6517.yml

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch exact-head governed Work Map autofix
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh workflow run spec-kit-work-map-autofix.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref main \
            -f branch=fix/response-chunk-push-bridge-runner-context-20260807 \
            -f expected_head_sha=de2c745ea2aa43665e7e03403b63672ef2f5fd14
